
#pragma warning(disable: 4996)

#include <iostream>
#include <stack> // in STL (Standard Template Library)
#include <vector> // in STL (Standard Template Library)
#include <queue> // in STL (Standard Template Library)
#include <map> // in STL (Standard Template Library)
#include <string>
#include "clsQueueLine.h"



using namespace std;
// shortcuts
// remove line (ctrl + l)
// solution explorer (ctrl + alt + l)
// add new header file (local library) (ctrl + shift + a)


int main() {








	return 0;
}